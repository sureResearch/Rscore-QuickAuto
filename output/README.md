# Output data

Program outputs may be written here, for example:

```powershell
python rscore_quickauto.py run `
  --input "input/HCM0001" `
  --output "output/HCM0001"
```

Generated patient-level files are excluded by `.gitignore` and must not be
committed to a public repository.

